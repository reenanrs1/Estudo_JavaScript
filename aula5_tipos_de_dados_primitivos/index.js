//String, number, undefined, null, boolean, symbol

const nome = 'Luiz'; //Pode ser com aspas simples/aspas duplas, pode-se utilizar também com crazi
const num1 = 10; //number
const num2 = 10.52 //number
let nomeAluno; // undefinded = não aponta pra local nenhuma na memória
let  sobrenomeAluno = null; //Nulo -: não aponta pra local nenhuma na memória
const aprovado = false; //Dados boolean = true, false (lógico)
const reprovado = true; //Dados boolean = true, false (lógico)

console.log( typeof nome, nome);

const a = [1, 2];
const b = a; //sinal de = seria atribuição, b atribui a, tornando iguais.

console.log(a, b);