/**
 *Aritméticos
 *+ Adição / Concatenação ( - / *)
 * **potenciação
 * % Resto da divisão
 * precdencia : (), **, *, /, %, +, -
 *  Incremento = ++
 *  Decremento = --
 *  NaN - Not a number... ParseInt(inteiro), parseFloat(Decimais), number(converte Inteiro, decimais)
 */

 const num1 = 5;
 const num2 = 10;
 console.log(num1 + num2)

 let contador = 1;
 contador++; // 2
 contador++; // 3
 contador++; // 4
 contador++; // 5
console.log('contador: ',contador);

const passo = 5;
let contador1 = 0;

contador1 = contador1 + passo;
console.log(contador1);
contador1 = contador1 + passo;
console.log(contador1);
contador1 = contador1 + passo;
console.log(contador1);